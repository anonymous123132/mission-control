#!/bin/sh
set -e

case "$1" in
  remove|upgrade|deconfigure)
    if command -v systemctl >/dev/null 2>&1; then
      systemctl stop mission-control.service || true
      systemctl disable mission-control.service || true
    fi
    ;;
esac

exit 0
