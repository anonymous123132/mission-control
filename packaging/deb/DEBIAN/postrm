#!/bin/sh
set -e

case "$1" in
  remove|purge)
    if command -v systemctl >/dev/null 2>&1; then
      systemctl daemon-reload || true
    fi
    ;;
esac

if [ "$1" = "purge" ]; then
  rm -f /etc/default/mission-control
fi

exit 0
